<UserControl x:Class="Seismoscope.View.DonneesCapteurView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Seismoscope.View" xmlns:viewmodel="clr-namespace:Seismoscope.ViewModel" d:DataContext="{d:DesignInstance Type=viewmodel:DonneesCapteurViewModel}"
             mc:Ignorable="d"
        Height="450" Width="800">
    <StackPanel  Background="WhiteSmoke">
        
        <TextBlock Margin="20, 20, 70, 20"
           Text="{Binding Nom, StringFormat=Données du Capteur {0}}"
           HorizontalAlignment="Center"
           VerticalAlignment="Center"
           FontWeight="Bold" />

        <StackPanel Margin="5">
           <!-- <DataGrid Width="300" ItemsSource="{Binding CsvDonnees}"  Margin="5" AutoGenerateColumns="True"/> -->
            <Button Width="300" Content="Sélectionnez" Command="{Binding LireCSVCommand}"  Margin="5"/>
        </StackPanel>
        
        <Grid Margin="10,5, 60,0" Width="700">
            
            <Grid Background="White" Margin="0,10,70,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="5*" />
                    
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="{Binding Type, StringFormat=Type d’onde : {0}}" Margin="10"/>
                <TextBlock Grid.Row="1" Text="{Binding Amplitude, StringFormat=Amplitude : {0} mm}" Margin="10"/>
                <StackPanel Grid.Row="2" Margin="5">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Button Margin="5" 
                                Grid.Column="0" 
                                Content="Lire" 
                                Command="{Binding CommencerLectureCommand}"
                                 IsEnabled="{Binding PeutCommencerLecture}"
                                Width="230" />
                        <Button Margin="5" 
                                Grid.Column="1" 
                                Content="Annuler"
                                Width="230"/>
                    </Grid>
                </StackPanel>
                <TextBlock FontWeight="Bold" Grid.Row="3" Text="Graphique" Margin="10"/>
                <TextBlock Grid.Row="4" Text="Graphique réel" Margin="10"/>
            </Grid>
            

        </Grid>
    </StackPanel>
    
</UserControl>

    