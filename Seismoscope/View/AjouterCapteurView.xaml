<Window x:Class="Seismoscope.View.AjouterCapteurView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Seismoscope.View"
        mc:Ignorable="d"
        Title="AjouterCapteurView" Height="450" Width="800">
    <StackPanel  Background="WhiteSmoke">
        <TextBlock Margin="30" 
                   Text="Ajouter Capteur" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center" 
                   FontWeight="Bold" />
        <ComboBox 
            Margin="30" 
            Height="30" Width="450"
            ItemsSource="{Binding Capteurs}" 
            DisplayMemberPath="Nom" 
            SelectedItem="{Binding CapteurSelectionne}" />

        <Button 
            Height="30" Width="350"
            Margin="50" 
            Content="Valider" 
            Command="{Binding ValiderCapteurCommand}" />
    </StackPanel>
    
</Window>
